apiVersion: v1
kind: Service
metadata:
  name: alloy
  labels:
    app.kubernetes.io/component: alloy
    app.kubernetes.io/part-of: vtdb-mcp
spec:
  selector:
    app.kubernetes.io/name: alloy
  ports:
    - name: http
      port: 12345
      targetPort: http
