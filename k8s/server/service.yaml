apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/part-of: vtdb-mcp
spec:
  selector:
    app.kubernetes.io/name: server
  ports:
    - name: http
      port: 8000
      targetPort: http
