apiVersion: v1
kind: Service
metadata:
  name: loki
  labels:
    app.kubernetes.io/component: loki
    app.kubernetes.io/part-of: vtdb-mcp
spec:
  selector:
    app.kubernetes.io/name: loki
  ports:
    - name: http
      port: 3100
      targetPort: http
